// src/services/api.ts (New folder/services - placeholder for future API, e.g., backend integration)
export const fetchInvoiceTemplates = async () => {
  // Placeholder: Fetch from backend
  return new Promise(resolve => resolve([]));
};

// You can expand this for real services later.